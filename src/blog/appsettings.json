{
  // Stored using dotnet user-secrets tool
  //"Database": {
  //  "ConnectionString":  "Server=Desktop-oba16gu;Database=Test;User Id=blog;Password=12345"
  //}
  // this will seed the database with an admin user if it doesn't exist yet
  // it should be changed so that the role isn't passed in like this, if its not Admins then you won't get an admin in te application
  "Data": {
    "AdminUser": {
      "Name": "Admin",
      "Email": "tylerlrhodes@gmail.com",
      "Password": "P@ssw0rd123",
      "Role": "Admins"
    }
  }
}
